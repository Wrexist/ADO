{
  "name": "ai-development-os",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first mission control for the Wrexist portfolio — the AI Development OS wedge (AI Control Center).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c magenta,cyan \"npm run dev -w @ado/server\" \"npm run dev -w @ado/web\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "echo \"lint: ESLint is wired in Phase 1 (see ROADMAP.md); Phase 0 relies on strict tsc + isolatedModules.\"",
    "test": "vitest run",
    "build": "npm run build --workspaces --if-present",
    "verify": "bash scripts/verify.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.0",
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
